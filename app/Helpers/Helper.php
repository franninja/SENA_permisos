<?php
namespace App\Helpers;  
use Illuminate\Support\Facades\DB;

class Helper{

    public static function uploads($disk, $filename){
        
        // $file = Storage::disk('challenge')->get($filename);
        // return new Response($file, 200);

        // no sirve pa na
    }
}